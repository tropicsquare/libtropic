name: Build examples for Raspberry Pi Pico boards
on:
  push:
    branches:
      - 'develop'
      - 'master'
  pull_request:
    branches:
      - 'master'
      - 'develop'

jobs:
  rpi_pico_build_examples:
    name: Build examples for Raspberry Pi Pico boards
    runs-on: ubuntu-latest
    env:
      PICO_SDK_PATH: ${{ github.workspace }}/pico-sdk
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.1.7

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
            sudo apt-get update
            sudo apt-get install -y cmake build-essential ninja-build gcc-arm-none-eabi libnewlib-arm-none-eabi git python3
            pip3 install jsonschema

      - name: Clone Pico SDK (pinned to 2.2.0) and verify SHA
        run: |
            git clone https://github.com/raspberrypi/pico-sdk.git $PICO_SDK_PATH
            git -C $PICO_SDK_PATH fetch --tags --prune
            git -C $PICO_SDK_PATH checkout 2.2.0
            git -C $PICO_SDK_PATH submodule update --init --recursive
            EXPECTED_SHA="a1438dff1d38bd9c65dbd693f0e5db4b9ae91779"
            ACTUAL_SHA=$(git -C $PICO_SDK_PATH rev-parse HEAD)
            echo "pico-sdk HEAD: $ACTUAL_SHA (expected $EXPECTED_SHA)"
            if [ "$ACTUAL_SHA" != "$EXPECTED_SHA" ]; then
              echo "ERROR: pico-sdk commit SHA mismatch"
              exit 1
            fi

      - name: Compile Raspberry Pi Pico examples
        run: |
          cd examples/rpi-pico/pico/hello_world
          cmake ./ -B build -G Ninja
          cd build && ninja

          cd ../../identify_chip
          cmake ./ -B build -G Ninja
          cd build && ninja

          cd ../../fw_update
          cmake ./ -B build -G Ninja -DLT_DISABLE_MAINTENANCE_MODE=OFF
          cd build && ninja

          cd ..
          cmake ./ -B build_dis_mm -G Ninja -DLT_DISABLE_MAINTENANCE_MODE=ON
          cd build_dis_mm && ninja

          cd ..
          cmake ./ -B build_abab -G Ninja -DLT_SILICON_REV=ABAB -DLT_DISABLE_MAINTENANCE_MODE=OFF
          cd build_abab && ninja

          cd ..
          cmake ./ -B build_abab_dis_mm -G Ninja -DLT_SILICON_REV=ABAB -DLT_DISABLE_MAINTENANCE_MODE=ON
          cd build_abab_dis_mm && ninja

      - name: Compile Raspberry Pi Pico 2 examples
        run: |
          cd examples/rpi-pico/pico2/hello_world
          cmake ./ -B build -G Ninja
          cd build && ninja

          cd ../../identify_chip
          cmake ./ -B build -G Ninja
          cd build && ninja

          cd ../../fw_update
          cmake ./ -B build -G Ninja -DLT_DISABLE_MAINTENANCE_MODE=OFF
          cd build && ninja

          cd ..
          cmake ./ -B build_dis_mm -G Ninja -DLT_DISABLE_MAINTENANCE_MODE=ON
          cd build_dis_mm && ninja

          cd ..
          cmake ./ -B build_abab -G Ninja -DLT_SILICON_REV=ABAB -DLT_DISABLE_MAINTENANCE_MODE=OFF
          cd build_abab && ninja

          cd ..
          cmake ./ -B build_abab_dis_mm -G Ninja -DLT_SILICON_REV=ABAB -DLT_DISABLE_MAINTENANCE_MODE=ON
          cd build_abab_dis_mm && ninja
